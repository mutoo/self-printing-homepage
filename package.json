{
  "name": "self-printing-homepage",
  "private": true,
  "version": "1.1.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "npm run build && vite public --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "bundle": "node scripts/bundle.mjs",
    "build": "npm run typecheck && npm run bundle",
    "preview": "npm run build && vite preview --outDir public --host 127.0.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
